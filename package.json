{
  "name": "codecompare",
  "version": "1.0.0",
  "description": "A simple web-based utility to compare two snippets of code or text, with syntax-highlighted diff output.",
  "private": true,
  "scripts": {
    "lint": "eslint script.js dashboard.js service-worker.js diff-utils.js share-utils.js json-utils.js",
    "format": "prettier --write \"**/*.{js,css,html,md}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{js,css,html,md}\" --ignore-path .gitignore",
    "test": "jest",
    "serve": "npx http-server -p 8080 -c-1"
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "jest": "^29.7.0",
    "prettier": "^3.3.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GeekNeuron/CodeCompare.git"
  },
  "license": "MIT"
}
